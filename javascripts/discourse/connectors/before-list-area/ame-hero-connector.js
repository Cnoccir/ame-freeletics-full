// Version 1.1 - Modern API
import { withPluginApi } from "discourse/lib/plugin-api";

export default {
  setupComponent(args, component) {
    // This connector simply renders the AmeHero component
    // Component logic is handled by the .hbs template
  }
};
